{
  "name": "unfeed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A calm front door for Trojan-horse sites — gate the feed route, keep the tool.",
  "scripts": {
    "test": "node --test"
  }
}
